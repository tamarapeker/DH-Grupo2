<link rel="stylesheet" href="/stylesheets/forms.css">

<%-include('../partials/header', {title: 'Hola ' + usuario.nombre})%>

<main>
    <h2>Cambiar contraseña </h2>
    <% if (typeof errors != "undefined"){ %>
        <ul style="padding: 0;">
            <% for (var i= 0; i< errors.length ; i++) { %>
                <li style="list-style: none; color:red"> <%= errors[i].msg %> </li>
            <%}%>
        </ul>
    <%}%>
    <% if(typeof errorAlLoguear != "undefined"){ %>
        <p style="color:red"><%= errorAlLoguear %></p>
    <% } %>
    <div>
        <form action="/users/password/<%=usuario.id%>" method="POST" class="forms">
            <input type="password" name="password" placeholder="Contraseña anterior">
            <input type="password" name="passwordNew" placeholder="Contraseña nueva">
            <input type="password" name="confirmPasswordNew" placeholder="Confirmar contraseña nueva">
            <button type="submit">Guardar cambios</button>
        </form>
    </div> 
</main>
<%-include('../partials/footer')%>